import styled from 'styled-components'

export const Blur = styled.div`
    position: absolute;
    width: 1013px;
    height: 1013px;
    left: -336px;
    top: -305px;

    background: #002853;
    filter: blur(282px);
    z-index: -1;
`